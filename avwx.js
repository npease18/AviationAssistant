var baseurl = "https://avwx.rest/api/"

function nearestStations(x,y) {
    // FetchPending = $.ajax({ url: baseurl + 'metar/'+x+','+y+'?token='+AVWX+'&options=info&format=json',
    //                                timeout: 5000,
    //                                cache: false,
    //                                dataType: 'json',
    //                                });
    //    FetchPending.done(function(data) {
    //        document.getElementById('metar_loading').style.display = "none"
    //        document.getElementById('metar_data').style.display = "block"
    //        json = data
    //        document.getElementById("metar_data").innerHTML = "Station ID: "+json.station
    //    })
    console.log("DISABLED TO RETAIN API SANITY")
}
